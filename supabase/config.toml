# Supabase Configuration
project_id = "avuedapgjitwnimkrjlp"

[api]
enabled = true
port = 54321
schemas = ["public"]
extra_search_path = ["public"]
max_rows = 1000

[db]
port = 54322
major_version = 15

[realtime]
enabled = true
ip_version = "IPv4"

[studio]
enabled = true
port = 54323

# Internal ERP: accounts are provisioned by an admin via the admin-users edge
# function, never by public self-registration. Keep signup disabled.
[auth]
enabled = true
site_url = "http://localhost:3000"
jwt_expiry = 3600
enable_signup = false

[auth.email]
enable_signup = false
enable_confirmations = false
